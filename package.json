{
  "name": "parcel",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --dir webapp dev",
    "build:web": "pnpm --dir webapp build",
    "lint:web": "pnpm --dir webapp lint",
    "dev:ios": "pnpm --dir ios start",
    "build:android": "pnpm --dir ios exec eas build --platform android",
    "install:all": "pnpm --dir webapp install && pnpm --dir ios install"
  }
}
